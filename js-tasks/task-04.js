function greet(name, owner) {
    return name === owner ? "Hello boss" : "Hello guest";
} //Любомир

function greet (name, owner) {
    if (name === owner) {
      return 'Hello boss';
    }
    else {
      return 'Hello guest';
    }
  } // Аня

  function greet (name, owner) {
    if( name === owner) {
      return 'Hello boss'
    }
      return 'Hello guest'
    } //Валентина