function move(position, roll) {
    return position + roll * 2;
} //Любомир

function move(position, roll) {
    return position + roll * 2;
} // Аня

function move(position, roll) {
    return position + (roll * 2)
} //Валентина