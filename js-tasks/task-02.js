function makeNegative(num) {
  if (num >= 0) {
    return -num;
  } else {
    return num;
  }
}// Любомир

function makeNegative(num) {
  if (num < 0) {
    return num;
  }
  else {
    return - num;
  }
} // Аня

function makeNegative(num) {
  return -Math.abs(num)
 } //Валентина