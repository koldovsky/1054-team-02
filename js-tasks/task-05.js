function litres(time) {
    return ~~(time * 0.5);
} //Любомир

function litres(time) {
    return ~~(time * 0.5);
  } // Аня

  function litres(time) {
    return Math.floor(time * 0.5);
  } //Валентина